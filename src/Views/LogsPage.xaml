<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TrustTunnelGui.Views.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="32" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Логи" FontSize="28" FontWeight="SemiBold"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <Button Content="Очистить" Click="Clear_Click"/>
            <Button Content="Сохранить в файл" Click="Save_Click"/>
            <ToggleSwitch x:Name="AutoScroll" IsOn="True" OnContent="Автопрокрутка" OffContent="Автопрокрутка"/>
        </StackPanel>

        <!-- ReadOnly TextBox: supports full multi-line text selection (Ctrl+A, drag, Shift+click)
             unlike ListView which only allows selecting text within a single item. -->
        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="4">
            <TextBox x:Name="LogBox"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="NoWrap"
                     FontFamily="Consolas"
                     FontSize="12"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     VerticalAlignment="Stretch"
                     HorizontalAlignment="Stretch"
                     VerticalContentAlignment="Top"/>
        </Border>
    </Grid>
</Page>
