<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TrustTunnelGui.Views.ServersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:m="using:TrustTunnelGui.Models">
    <Grid Padding="32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Серверы" FontSize="28" FontWeight="SemiBold"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <Button Content="Добавить" Click="Add_Click" Style="{ThemeResource AccentButtonStyle}"/>
            <Button Content="Импорт TOML" Click="Import_Click"
                    ToolTipService.ToolTip="Импортировать профиль из локального .toml файла"/>
            <Button Content="Импорт по URL" Click="ImportUrl_Click"
                    ToolTipService.ToolTip="Скачать и импортировать профиль по прямой ссылке на .toml (например raw GitHub или ссылка от сервера)"/>
        </StackPanel>

        <ListView Grid.Row="2" x:Name="List" SelectionMode="None">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0,4"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="m:ServerProfile">
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            CornerRadius="8" Padding="16">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{x:Bind Name, Mode=OneWay}" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind Hostname, Mode=OneWay}" Opacity="0.6" FontSize="12"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="Изменить" Tag="{x:Bind}" Click="Edit_Click"/>
                            <Button Grid.Column="2" Content="Удалить"  Tag="{x:Bind}" Click="Delete_Click"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
