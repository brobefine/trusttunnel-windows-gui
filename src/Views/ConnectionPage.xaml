<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TrustTunnelGui.Views.ConnectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="TrustTunnel" FontSize="32" FontWeight="SemiBold"/>

        <!-- Server selector -->
        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="16">
            <StackPanel Spacing="8">
                <TextBlock Text="Активный сервер" Opacity="0.7" FontSize="12"/>
                <ComboBox x:Name="ProfileBox" MinWidth="320"
                          DisplayMemberPath="Name"
                          SelectionChanged="ProfileBox_SelectionChanged"/>
            </StackPanel>
        </Border>

        <!-- Status + connect buttons -->
        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="20">
            <StackPanel Spacing="12">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <FontIcon x:Name="StatusIcon" Glyph="&#xE73E;" FontSize="24" Foreground="Gray"/>
                    <TextBlock x:Name="StatusText" Text="Отключено" FontSize="20" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button x:Name="ConnectBtn" Content="Подключить" Click="Connect_Click"
                            Style="{ThemeResource AccentButtonStyle}"/>
                    <Button x:Name="DisconnectBtn" Content="Отключить" Click="Disconnect_Click" IsEnabled="False"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Warning bar -->
        <InfoBar x:Name="WarnBar" Grid.Row="3" Severity="Warning" IsClosable="False" IsOpen="False"/>

        <!-- Log toolbar -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="8">
            <Button Content="Очистить" Click="Clear_Click"/>
            <Button Content="Сохранить в файл" Click="Save_Click"/>
            <ToggleSwitch x:Name="AutoScroll" IsOn="True"
                          OnContent="Автопрокрутка" OffContent="Автопрокрутка"/>
        </StackPanel>

        <!-- Full log TextBox -->
        <Border Grid.Row="5" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="4">
            <TextBox x:Name="LogBox"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="NoWrap"
                     FontFamily="Consolas"
                     FontSize="12"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     VerticalAlignment="Stretch"
                     HorizontalAlignment="Stretch"
                     VerticalContentAlignment="Top"/>
        </Border>
    </Grid>
</Page>
